# /copymf — Processo Nicolas Toigo — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Instalar as 4 skills de copywriting do Nicolas Toigo e criar um comando orquestrador `/copymf` que as conecta numa sequência menu-driven (produção VSL/Anúncio → gates de revisão), com persistência própria de marca e output em pt-BR.

**Architecture:** As 4 skills `.skill` (ZIPs) são extraídas para `~/.claude/skills/`. Um comando fino em `~/.claude/commands/copymf.md` (persona Nicolas Toigo) gerencia marca, intake e roteamento, acionando as skills via Skill tool. Persistência em arquivos markdown sob `Megabrain MeuFluxo/copymf/`. Nenhum código executável — entregáveis são markdown e arquivos de skill.

**Tech Stack:** Markdown (skills + comando), arquivos `.skill` (ZIP), shell (`unzip`) para instalação. Sem framework de testes — verificação por inspeção de arquivos e smoke-test do fluxo.

**Spec de referência:** [docs/superpowers/specs/2026-06-24-copymf-workflow-design.md](../specs/2026-06-24-copymf-workflow-design.md)

---

## Convenção de verificação (este projeto não tem test runner)

Como não há `pytest`/`npm test`, cada tarefa tem uma **verificação observável**: um comando shell que comprova o resultado (arquivo existe, frontmatter presente, slug correto) e a saída esperada. Trate "Expected" como o critério de PASS.

Não há git neste diretório (não é repo). Os passos de "Commit" são **opcionais** e só se aplicam se o usuário rodar `git init` antes. Onde aparecem, estão marcados `(opcional — só com git)`. O agente NÃO deve rodar `git init` por conta própria.

---

## File Structure

Arquivos criados/modificados por este plano:

```
~/.claude/skills/vsl-copywriter-br/SKILL.md        # Create (extraído)
~/.claude/skills/ad-strategist/SKILL.md            # Create (extraído)
~/.claude/skills/ad-copy-reviewer/SKILL.md         # Create (extraído)
~/.claude/skills/google-ads-reviewer/SKILL.md      # Create (extraído)
~/.claude/commands/copymf.md                       # Create (orquestrador)

Megabrain MeuFluxo/copymf/.active-brand            # Runtime (criado pelo orquestrador na 1ª marca; NÃO é scaffold)
Megabrain MeuFluxo/copymf/brands/INDEX.md          # Create (tabela vazia)
Megabrain MeuFluxo/copymf/brands/_TEMPLATE.md      # Create (template Brand Context)
Megabrain MeuFluxo/copymf/output/.gitkeep          # Create
```

Responsabilidades:
- **4 SKILL.md**: metodologia de produção/revisão (fonte de verdade; não editados, só instalados).
- **copymf.md**: orquestrador fino — menu, intake, roteamento, sequência de gates, persistência.
- **brands/_TEMPLATE.md**: contrato do Brand Context (interface entre persistência e skills).
- **brands/INDEX.md**: índice de unicidade de marcas.

---

## Chunk 1: Instalação das 4 skills

### Task 1: Extrair as 4 skills para ~/.claude/skills/

**Files:**
- Create: `~/.claude/skills/vsl-copywriter-br/SKILL.md`
- Create: `~/.claude/skills/ad-strategist/SKILL.md`
- Create: `~/.claude/skills/ad-copy-reviewer/SKILL.md`
- Create: `~/.claude/skills/google-ads-reviewer/SKILL.md`
- Source: `~/Downloads/IA/{VSL-Copywriter,ADS-Copywriter,CopyChief-Reviewer,Copywriter-GoogleADS}.skill`

- [ ] **Step 1: Garantir que o diretório de skills existe**

Run:
```bash
mkdir -p ~/.claude/skills
```
Expected: sem erro.

- [ ] **Step 2: Extrair cada `.skill` (ZIP) para ~/.claude/skills/**

Cada `.skill` contém uma pasta `<nome-interno>/SKILL.md`. Extrair direto preserva a pasta com o nome interno correto.

Run:
```bash
cd ~/.claude/skills
unzip -o ~/Downloads/IA/VSL-Copywriter.skill
unzip -o ~/Downloads/IA/ADS-Copywriter.skill
unzip -o ~/Downloads/IA/CopyChief-Reviewer.skill
unzip -o ~/Downloads/IA/Copywriter-GoogleADS.skill
```
Expected: 4 pastas criadas — `vsl-copywriter-br/`, `ad-strategist/`, `ad-copy-reviewer/`, `google-ads-reviewer/`.

- [ ] **Step 3: Verificar que os 4 SKILL.md existem nos nomes internos corretos**

Run:
```bash
ls ~/.claude/skills/vsl-copywriter-br/SKILL.md \
   ~/.claude/skills/ad-strategist/SKILL.md \
   ~/.claude/skills/ad-copy-reviewer/SKILL.md \
   ~/.claude/skills/google-ads-reviewer/SKILL.md
```
Expected: as 4 linhas listadas sem "No such file".

- [ ] **Step 4: Verificar frontmatter `name:` de cada skill**

Run:
```bash
for d in vsl-copywriter-br ad-strategist ad-copy-reviewer google-ads-reviewer; do
  echo "== $d =="; grep -m1 '^name:' ~/.claude/skills/$d/SKILL.md
done
```
Expected:
```
== vsl-copywriter-br ==
name: vsl-copywriter-br
== ad-strategist ==
name: ad-strategist
== ad-copy-reviewer ==
name: ad-copy-reviewer
== google-ads-reviewer ==
name: google-ads-reviewer
```

- [ ] **Step 5: Confirmar que `ad-copy-reviewer` aceita roteiro de VSL (nota de verificação do spec, Seção 4.5)**

Run:
```bash
grep -i 'roteiro\|video script\|video sales\|VSL' ~/.claude/skills/ad-copy-reviewer/SKILL.md | head
```
Expected: ao menos uma linha confirmando que a skill cobre "video scripts (roteiros)". Se vazio, registrar no plano que o Gate 1 da trilha VSL precisa de fallback (não bloqueia esta task).

---

## Chunk 2: Scaffold de persistência do projeto

### Task 2: Criar a estrutura de pastas `copymf/`

**Files:**
- Create: `Megabrain MeuFluxo/copymf/output/.gitkeep`
- Create: `Megabrain MeuFluxo/copymf/brands/` (via arquivos abaixo)

- [ ] **Step 1: Criar pastas base**

Run:
```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
mkdir -p copymf/brands copymf/output
touch copymf/output/.gitkeep
```
Expected: sem erro; `copymf/brands` e `copymf/output` existem.

- [ ] **Step 2: Verificar estrutura**

Run:
```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
find copymf -type d
```
Expected:
```
copymf
copymf/brands
copymf/output
```

### Task 3: Criar o template de Brand Context

**Files:**
- Create: `Megabrain MeuFluxo/copymf/brands/_TEMPLATE.md`

- [ ] **Step 1: Escrever o template** (interface Brand Context → skills, conforme Spec 4.4)

Conteúdo de `copymf/brands/_TEMPLATE.md`:
```markdown
---
slug: <kebab-case>
nome: <Nome da marca/produto>
criado_em: AAAA-MM-DD
---

# Brand Context — <Nome>

## Avatar
- Idade / gênero:
- Dor principal:
- Tentativas que já falharam:
- Linguagem que usa (palavras/expressões exatas):
- Estado emocional dominante:

## Oferta
- O que é (digital/físico/serviço):
- Formato de entrega:

## Mecanismo único (UMP)
- Nome do mecanismo:
- Por que funciona quando o resto falhou:

## Preço
- Valor / parcelamento:
- Justificativa de preço:

## Prova social
- Depoimentos / antes-e-depois / dados disponíveis:

## Âncora de comparação
- O que o avatar já conhece e gasta (ex: concorrente, medicamento, academia):

## Objeções principais
1.
2.
3.

## Sofisticação / Awareness
- Nível de mercado (Schwartz 1-5):
- Onde o avatar está na jornada de awareness:
```

- [ ] **Step 2: Verificar frontmatter e seções**

Run:
```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
grep -c '^## ' copymf/brands/_TEMPLATE.md
head -1 copymf/brands/_TEMPLATE.md
```
Expected: contagem de seções `## ` = 8; primeira linha = `---`.

### Task 4: Criar o INDEX de marcas

**Files:**
- Create: `Megabrain MeuFluxo/copymf/brands/INDEX.md`

- [ ] **Step 1: Escrever o INDEX vazio**

Conteúdo de `copymf/brands/INDEX.md`:
```markdown
# Marcas — /copymf

Índice de unicidade. Slug deve ser único (kebab-case). Marca ativa fica em `../.active-brand`.

| Slug | Nome | Criado em | Entregas |
|------|------|-----------|----------|
```

- [ ] **Step 2: Verificar**

Run:
```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
grep -q '| Slug | Nome |' copymf/brands/INDEX.md && echo OK
```
Expected: `OK`.

---

## Chunk 3: O orquestrador /copymf

### Task 5: Escrever o comando orquestrador

**Files:**
- Create: `~/.claude/commands/copymf.md`

- [ ] **Step 1: Escrever `copymf.md`** com o conteúdo completo abaixo

```markdown
---
name: copymf
description: >
  Processo Nicolas Toigo de copy do MeuFluxo. Orquestrador menu-driven que conecta as 4 skills
  de copywriting (vsl-copywriter-br, ad-strategist, ad-copy-reviewer, google-ads-reviewer) numa
  sequência: produção (VSL ou Anúncio) → Gate de qualidade (CopyChief) → Gate de compliance
  (Google/YT, condicional) → entrega. Persistência própria de marca. Output sempre em pt-BR.
  Use quando o copywriter quiser produzir VSL, produzir anúncios (Meta/Google), otimizar um
  anúncio vencedor, ou revisar copy pronta dentro do fluxo MeuFluxo.
---

# /copymf — Nicolas Toigo, Chief Copywriter do MeuFluxo

Você é **Nicolas Toigo**, o maior copywriter brasileiro e fundador do MeuFluxo, atuando como
chief copywriter. Você conduz um processo: classifica o pedido, roteia para a skill certa,
e conduz os gates de revisão. **Todo output é em pt-BR**, sempre. As skills-fonte são em inglês;
você aplica a metodologia e entrega em português.

## Constantes
- `PROJ` = diretório do projeto Megabrain MeuFluxo (cwd atual deve ser ele).
- `COPYMF` = `$PROJ/copymf`
- `ACTIVE` = `$COPYMF/.active-brand` (slug da marca ativa, 1 linha)
- `BRANDS` = `$COPYMF/brands/` — `<slug>.md` por marca, `INDEX.md`, `_TEMPLATE.md`
- `OUTPUT` = `$COPYMF/output/<slug>/`
- Skills acionadas via Skill tool por nome interno:
  - Produção VSL → `vsl-copywriter-br`
  - Produção Anúncio → `ad-strategist`
  - Gate 1 (qualidade) → `ad-copy-reviewer`
  - Gate 2 (compliance Google/YT) → `google-ads-reviewer`

## Passo 0 — Resolver marca ativa
1. Se `$ACTIVE` existe e aponta para um `$BRANDS/<slug>.md` válido → carregue esse Brand Context.
2. Senão → mostre marcas existentes (de `INDEX.md`) e ofereça **criar** ou **selecionar**.
3. Ao criar: derive slug kebab-case do nome; se já existir no `INDEX.md`, sufixe `-2`, `-3`…
   e confirme. Preencha a partir de `_TEMPLATE.md`, perguntando só o essencial. Atualize `INDEX.md`
   e grave o slug em `$ACTIVE`.

## Passo 1 — Menu
Apresente:
```
[1] Produzir VSL
[2] Produzir Anúncio
[3] Revisar copy pronta
[4] Gerenciar marcas (criar / trocar / editar / duplicar)
```
Se o copywriter escrever um pedido em linguagem livre, **classifique a intenção** (VSL / Anúncio /
revisão / gestão de marca) e siga a trilha correspondente, confirmando a leitura em 1 linha.

## Trilha [1] — VSL
1. **Intake:** Canal (Meta · Google/YouTube). (VSL é peça única; sem "quantidade".)
2. **Produção:** acione `vsl-copywriter-br` via Skill tool, injetando o Brand Context como fase
   Research/Brief já preenchida (peça só o que faltar). Deixe a skill rodar bloco-a-bloco.
3. **Gate 1 — CopyChief:** acione `ad-copy-reviewer` sobre o roteiro (a skill cobre roteiros de VSL;
   se uma versão futura restringir a ads curtos, revise por blocos do roteiro como fallback).
   Mostre ≤3 problemas + reescrita pronta. **Pergunte "aplico?"** Aplique só se confirmado; se
   rejeitado, siga com o original.
4. **Gate 2 — GoogleADS:** SÓ se Canal = Google/YouTube. Acione `google-ads-reviewer`. Mesma
   mecânica "revisar e perguntar".
5. **Entrega:** salve em `$OUTPUT` (ver Passo Entrega).

## Trilha [2] — Anúncio
1. **Intake:**
   - Quantidade (nº de anúncios, ex: 1/5/10)
   - Canal (Meta · Google/YouTube) — aciona/dispensa Gate 2
   - Tipo (Novo · Otimização de vencedor)
2. **Produção** via `ad-strategist`, injetando Brand Context como Stage 1 (Knowledge Base); o campo
   sofisticação/awareness também guia a ideação/ângulo (Stage 2/3), não só a Knowledge Base:
   - **Novo** → Stages 1-3 (Foundations → Ideação → Roteiros). Gere N anúncios puxando de fontes
     DIVERSAS (swipes/templates/organic/ângulos), não N variações do mesmo ângulo.
   - **Otimização** → peça o anúncio vencedor (cole). Se não vier, **bloqueie** e peça antes de seguir.
     Rode Stage 5 (Segment / Mechanism / C.A.S.H. Swap) → N variações.
3. **Gate 1 — CopyChief** (`ad-copy-reviewer`): sempre, sobre o conjunto. "Revisar e perguntar".
4. **Gate 2 — GoogleADS** (`google-ads-reviewer`): só se Canal = Google/YouTube. "Revisar e perguntar".
5. **Entrega:** 1 arquivo com as N variações (cada uma `### Anúncio N`).

## Trilha [3] — Revisar copy pronta
1. Copywriter cola a copy.
2. Pergunte o Canal (decide Gate 2).
3. Gate 1 CopyChief → Gate 2 GoogleADS (condicional) → entregue a versão revisada (salvar é opcional;
   pergunte se quer salvar em `$OUTPUT`).

## Trilha [4] — Gerenciar marcas
Criar / trocar (atualiza `$ACTIVE`) / editar `<slug>.md` / duplicar (novo slug, copia conteúdo).
Mantenha `INDEX.md` sincronizado e unicidade de slug.

## Mecânica dos Gates ("revisar e perguntar")
- O revisor roda e apresenta problemas (máx 3, formato Luke Iha) + reescrita pronta.
- **Sempre pergunte antes de aplicar.** "Não aplico" → mantém original, segue o fluxo (não trava).
- O copywriter pode pedir outro ciclo. Sem auto-loop forçado.
- Ordem fixa: CopyChief (qualidade) ANTES de GoogleADS (compliance).

## Passo Entrega — salvar em disco
1. `<slug>` = marca ativa; `<tipo>` ∈ {vsl, ad}.
2. `<id>` determinístico: escaneie `$OUTPUT` por `AAAA-MM-DD-<tipo>-*`; `<id>` = maior sufixo + 1
   (começa em 1). Data = hoje.
3. Salve em `$OUTPUT/AAAA-MM-DD-<tipo>-<id>.md`. Crie a pasta se não existir.
4. Atualize a coluna "Entregas" da marca em `INDEX.md`.
5. Confirme o caminho salvo ao copywriter.

## Regras invioláveis
- Output sempre pt-BR.
- Nunca aplicar reescrita de gate sem confirmação.
- Nunca pular o Gate 1 (CopyChief). Gate 2 só com Canal Google/YT.
- Brand Context é a fonte; não recolete o que já está em `<slug>.md`.
```

- [ ] **Step 2: Verificar frontmatter e seções-chave do comando**

Run:
```bash
head -1 ~/.claude/commands/copymf.md
grep -c '^name: copymf' ~/.claude/commands/copymf.md
grep -E '^## (Trilha|Passo|Menu|Mec)' ~/.claude/commands/copymf.md
```
Expected: primeira linha `---`; `name: copymf` presente (1); lista contendo as trilhas [1]-[4], Passo 0/1/Entrega e a Mecânica dos Gates.

- [ ] **Step 3: Verificar referência às 4 skills pelos nomes internos**

Run:
```bash
for s in vsl-copywriter-br ad-strategist ad-copy-reviewer google-ads-reviewer; do
  grep -q "$s" ~/.claude/commands/copymf.md && echo "ok $s" || echo "FALTA $s"
done
```
Expected: 4 linhas `ok ...`, nenhuma `FALTA`.

---

## Chunk 4: Smoke-test do fluxo (verificação ponta-a-ponta)

### Task 6: Validar que o processo está pronto e coeso

Não executa copy real — verifica que todas as peças existem e se referenciam corretamente.

- [ ] **Step 1: Checklist de artefatos**

Run:
```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
echo "skills:"; ls ~/.claude/skills/{vsl-copywriter-br,ad-strategist,ad-copy-reviewer,google-ads-reviewer}/SKILL.md
echo "comando:"; ls ~/.claude/commands/copymf.md
echo "persistencia:"; ls copymf/brands/_TEMPLATE.md copymf/brands/INDEX.md copymf/output/.gitkeep
```
Expected: todos os caminhos listados sem erro.

- [ ] **Step 2: Coerência cruzada — comando ↔ skills instaladas**

Run:
```bash
for s in $(grep -oE '(vsl-copywriter-br|ad-strategist|ad-copy-reviewer|google-ads-reviewer)' ~/.claude/commands/copymf.md | sort -u); do
  test -f ~/.claude/skills/$s/SKILL.md && echo "ok $s instalada" || echo "ERRO $s referenciada mas nao instalada"
done
```
Expected: 4 linhas `ok ... instalada`.

- [ ] **Step 3: Dry-run mental do fluxo (revisão humana)**

Percorra o `copymf.md` confirmando, por leitura:
- Passo 0 resolve/cria marca antes de produzir.
- Trilha Anúncio coleta Quantidade + Canal + Tipo; "Otimização" bloqueia sem anúncio vencedor.
- Gate 1 sempre; Gate 2 condicional ao Canal; ordem CopyChief → GoogleADS.
- Gates perguntam antes de aplicar; rejeição segue com o original.
- Entrega usa a regra do `<id>` e salva em `output/<slug>/`.

Expected: todos os itens presentes no texto do comando.

- [ ] **Step 4 (opcional — só com git): Commit do processo**

```bash
cd "/Users/andrexavier.ads/Documents/Megabrain MeuFluxo"
git add copymf docs/superpowers/specs/2026-06-24-copymf-workflow-design.md docs/superpowers/plans/2026-06-24-copymf.md
git commit -m "feat: processo /copymf (Nicolas Toigo) — 4 skills + orquestrador"
```
Nota: as skills e o comando vivem em `~/.claude/` (fora do projeto) e não entram neste commit.

---

## Resumo das tarefas

| Task | Entrega | Verificação |
|------|---------|-------------|
| 1 | 4 skills extraídas em `~/.claude/skills/` | nomes internos + frontmatter corretos |
| 2 | pastas `copymf/brands` e `copymf/output` | `find` mostra 3 dirs |
| 3 | `brands/_TEMPLATE.md` (Brand Context) | 8 seções + frontmatter |
| 4 | `brands/INDEX.md` | cabeçalho da tabela presente |
| 5 | `~/.claude/commands/copymf.md` | frontmatter + trilhas + 4 skills referenciadas |
| 6 | smoke-test ponta-a-ponta | coerência comando ↔ skills |
